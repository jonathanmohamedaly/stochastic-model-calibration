[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stochastic-model-calibration"
version = "0.1.0"
description = "Quantitative finance calibration project"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["src*"]